# Ascend-Inference-wiki 仓库规约

## 目录结构

```text
docs/          正式文档（经验总结，质量稳定后放这里）
  ├── debug/       调测经验总结（无任务编号，英文命名）
  ├── design/      设计方案
  └── practice/    实践指南
debug/         草稿区（可以有任务编号、非英文命名，未完善的内容先放这里）
```

## 文件命名

- `docs/` 下统一使用 **英文** 命名，全小写 + 短横线
  - ✅ `environment-troubleshooting-guide.md`
  - ❌ `环境调试踩坑记录.md`
  - ❌ `A00275_环境调试踩坑记录.md`
- `debug/` 下无命名约束，可以有中文和任务编号
  - 草稿完成后需要清理编号、改为英文名再移入 `docs/`

## 文档规约

- 每篇 .md 文件在写入/修改后需运行 `markdownlint`，报错全修完再提交
- 文档中**不含**敏感信息（账号、密码、内网 IP 等）
- 涉及具体任务单号的内容放在 `debug/` 下，不进入 `docs/`

## 内容质量

- 问题描述要包含**错误日志**和**原因分析**
- 解决方案要包含**可复制的命令**
- 标注阅读目标：适合什么场景下看这篇文档
