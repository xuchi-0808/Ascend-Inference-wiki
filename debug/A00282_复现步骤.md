# A00282 — 绿区诊断打点手册

> 2026-06-22 更新。PTA 升级后 `print_npugraph_tensor` 仍不可用，改用 `save_npugraph_tensor`。
> 已排除：mc2_mask stale（普通 print 证明不在 capture scope）、prefix-caching、量化、decode cudagraph。
> 新可疑点：`self.num_tokens` 实例属性跨 prepare→finalize 传递，在 capture scope 内可能 stale。
> 协作模式：AI 写文档，用户在内网绿区（`80.48.37.120/121`）手操。

## TL;DR

- **已排除的方向**：mc2_mask stale（不在 capture scope）、prefix-caching、量化（BF16/W8A8 都挂）、decode cudagraph（FULL_DECODE_ONLY 不恢复）、moe_forward vs moe_forward_shared（MC2 核心逻辑共享）
- **新可疑点**：`PrepareAndFinalizeWithMC2.prepare` 里设置的 `self.num_tokens`（Python int 实例属性），在 `finalize` 里用于 slice。如果 prepare/finalize 都在 capture scope 内，replay 时 `self.num_tokens` 可能 stale
- **触发条件**：只有全 NONE（E3）恢复精度；单并发对、多并发错
- **关键配置对比**：nightly A2 里只有 Qwen3 无 speculative decode，其他模型（DeepSeek-R1/GLM-5.1/Kimi-K2.5）都有 speculative
- **下一步**：用 `save_npugraph_tensor` 在 MC2 路径关键节点保存 tensor，定位哪个环节的值异常

## 实验矩阵

| # | 改动 | 结果 | 含义 |
|---|------|------|------|
| E1 | 强制 ALLGATHER 绕开 MC2 | ✅ 绿区恢复 | MC2 路径是根因 |
| E2 | 恢复 TP all-reduce | ❌ 不恢复 | reduce 逻辑无关 |
| E3 | 关 cudagraph（`cudagraph_mode=NONE`） | ✅ 绿区恢复 | cudagraph 是必要条件 |
| E4 | 强制 A3 走 MC2 | ❌ 不复现 | A3 环境差异 |
| E5 | tensor 比较替代 in-place | ❌ crash (507011) | 实现有问题 |
| E6a | 普通 print 诊断 mc2_mask | ✅ 确认 mask 不 stale | mc2_mask 排除 |
| E6b | FULL_DECODE_ONLY | ❌ 不恢复 | decode cudagraph 非关键 |
| E6c | 关 prefix-caching | ❌ 不恢复 | prefix-caching 排除 |
| **E7** | **save_npugraph_tensor 图内诊断** | **⏳ 待跑** | **定位 MC2 路径哪个环节异常** |

## E7：图内诊断 — save_npugraph_tensor（当前重点）

### 目的

在 MC2 路径的关键节点用 `torch_npu.save_npugraph_tensor` 保存 tensor 到文件，拿 replay 时的真实值，定位哪个环节输出了问题。

### 前提

`save_npugraph_tensor` 是 PTA 提供的 tensor dump 接口，支持 aclgraph 模式——save 操作会被图捕获，**replay 时也会触发保存**。比普通 `torch.save` 可靠（后者在 capture scope 内会断图）。

### 接口说明

```python
torch_npu.save_npugraph_tensor(input, save_path=None, overwrite=False) -> None
```

- `input`：tensor 或 tensor list
- `save_path`：文件路径（含文件名），如 `/home/xuchi/A00282_tensors/mc2_mask.pt`
- `overwrite=False`：**自动加计数标识**，文件名变成 `mc2_mask_device_0_0.pt`、`mc2_mask_device_0_1.pt`... 不覆盖

> 8 张卡各自保存（文件名含 device 序号），每次 forward 递增计数标识。不需要手动编号。

### 目录准备

```bash
mkdir -p /home/xuchi/A00282_tensors
```

### save 点（3 个必选 + 2 个可选）

所有 save 路径统一前缀 `/home/xuchi/A00282_tensors/`。

#### save 点 1：mc2_mask（dispatch 前）— 必选

**文件**：`vllm_ascend/ops/fused_moe/token_dispatcher.py`
**位置**：`TokenDispatcherWithMC2.get_dispatch_mc2_kwargs` 方法，`return kwargs_mc2` 之前

```python
        # A00282-diag: save mc2_mask
        import torch_npu
        if self.global_bs == 0:
            torch_npu.save_npugraph_tensor(
                token_dispatch_input.routing.mc2_mask,
                save_path="/home/xuchi/A00282_tensors/mc2_mask.pt",
                overwrite=False,
            )
        return kwargs_mc2
```

> save 完整 mask（bool tensor，很小）。replay 后用 `torch.load()` 看 mask 里 True/False 的分布。

#### save 点 2：prepare 的 num_tokens — 必选

**文件**：`vllm_ascend/ops/fused_moe/prepare_finalize.py`
**位置**：`PrepareAndFinalizeWithMC2.prepare` 方法，`return MoEPrepareOutput` 之前

```python
        # A00282-diag: save prepare num_tokens
        import torch_npu
        torch_npu.save_npugraph_tensor(
            torch.tensor(self.num_tokens, device=hidden_states.device).unsqueeze(0).to(torch.int32),
            save_path="/home/xuchi/A00282_tensors/prepare_num_tokens.pt",
            overwrite=False,
        )
        return MoEPrepareOutput(
            ...
```

#### save 点 3：finalize 的 num_tokens — 必选

**文件**：`vllm_ascend/ops/fused_moe/prepare_finalize.py`
**位置**：`PrepareAndFinalizeWithAll2All.finalize` 方法，`return hidden_states` 之前

```python
        # A00282-diag: save finalize num_tokens
        import torch_npu
        torch_npu.save_npugraph_tensor(
            torch.tensor(self.num_tokens, device=hidden_states.device).unsqueeze(0).to(torch.int32),
            save_path="/home/xuchi/A00282_tensors/finalize_num_tokens.pt",
            overwrite=False,
        )
        return hidden_states
```

> save 点 2 和 3 对比：如果 prepare 和 finalize 的值不一致 → 确认实例属性 stale。

#### save 点 4：dispatch 输出 — 可选

**文件**：`vllm_ascend/ops/fused_moe/token_dispatcher.py`
**位置**：`TokenDispatcherWithMC2.token_dispatch` 方法，`return MoETokenDispatchOutput` 之前

```python
        # A00282-diag: save dispatch output
        import torch_npu
        torch_npu.save_npugraph_tensor(
            expand_x.sum(dim=-1).unsqueeze(0).to(torch.float32),
            save_path="/home/xuchi/A00282_tensors/dispatch_out.pt",
            overwrite=False,
        )
```

#### save 点 5：combine 输出 — 可选

**文件**：`vllm_ascend/ops/fused_moe/token_dispatcher.py`
**位置**：`TokenDispatcherWithMC2.token_combine` 方法，`return combined_output` 之前

```python
        # A00282-diag: save combine output
        import torch_npu
        torch_npu.save_npugraph_tensor(
            combined_output.sum(dim=-1).unsqueeze(0).to(torch.float32),
            save_path="/home/xuchi/A00282_tensors/combine_out.pt",
            overwrite=False,
        )
```

### 操作步骤（两节点都改）

```bash
cd /vllm-workspace/vllm-ascend

# 1. 回退到干净状态（如果之前改过）
git checkout vllm_ascend/ops/fused_moe/token_dispatcher.py
git checkout vllm_ascend/ops/fused_moe/prepare_finalize.py
git checkout vllm_ascend/ascend_forward_context.py

# 2. 创建 save 目录
mkdir -p /home/xuchi/A00282_tensors

# 3. 在上述位置加 save_npugraph_tensor

# 4. 语法检查
python3 -c "import ast; ast.parse(open('vllm_ascend/ops/fused_moe/token_dispatcher.py').read()); print('token_dispatcher OK')"
python3 -c "import ast; ast.parse(open('vllm_ascend/ops/fused_moe/prepare_finalize.py').read()); print('prepare_finalize OK')"

# 5. 重装
pip install -e .
```

### 重起 + 验证

1. **Node 1（Worker）先起** — 见下方「Node 1（Worker/headless）」的 serve 命令
2. **Node 0（Leader）后起** — 见下方「Node 0（Leader）」的 serve 命令
3. 等 Node 0 日志出现 `Starting vLLM server on http://0.0.0.0:8080`
4. 跑 [32 并发验证](#32-并发验证)，**跑几个请求后尽快停**（避免文件过多）

### 判读

跑完后在 Node 0 上看文件：

```bash
ls /home/xuchi/A00282_tensors/ | head -30
ls /home/xuchi/A00282_tensors/ | wc -l
```

文件名格式：`<name>_device_<rank>_<counter>.pt`。counter 从 0 递增：
- counter 小（0, 1, 2...）：capture 阶段（每个 capture_size 一次）
- counter 大：replay 阶段（每次 forward）

用 Python 读文件看值：

```python
import torch
import glob

# 读 prepare_num_tokens 的所有文件（weights_only=True：只读 tensor，防注入）
for f in sorted(glob.glob("/home/xuchi/A00282_tensors/prepare_num_tokens_device_0_*.pt")):
    val = torch.load(f, weights_only=True)
    print(f.split("/")[-1], val.item())

# 对比 finalize_num_tokens
print("=== finalize ===")
for f in sorted(glob.glob("/home/xuchi/A00282_tensors/finalize_num_tokens_device_0_*.pt")):
    val = torch.load(f, weights_only=True)
    print(f.split("/")[-1], val.item())
```

| 观察 | 含义 | 结论 |
|------|------|------|
| `prepare_num_tokens` ≠ `finalize_num_tokens`（同一 counter） | 实例属性 stale | **self.num_tokens 是根因** |
| `prepare_num_tokens` replay 时**固定不变** | 被 capture 固化 | capture scope 确认 |
| `prepare_num_tokens` replay 时**随 batch 变化** | 正常更新 | num_tokens 排除 |
| `mc2_mask` replay 时 True 数量**固定** | mask stale | mc2_mask 是根因 |
| `mc2_mask` replay 时 True 数量**变化** | mask 正常 | mc2_mask 排除 |
| `dispatch_out` 全 0 | dispatch 丢 token | MC2 dispatch 算子问题 |

### 文件清理

文件会快速增长（每次 forward × 5 点 × 8 卡）。跑完分析后清理：

```bash
rm -f /home/xuchi/A00282_tensors/*.pt
```

### 拿到数据后

把读出来的值贴给我（或截取 capture 阶段 + replay 阶段各几十条），我来分析。

- 如果 `prepare_num_tokens` ≠ `finalize_num_tokens` → 确认 self.num_tokens stale，设计修复（改用 device tensor 传递）
- 如果 MC2 dispatch/combine 输出异常 → 问题在 C++ 算子层面，需要进一步排查
- 如果所有值都正常但输出仍乱码 → 问题在更下游（finalize 的 slice/reduce 或 MoE runner 的 reduce）

### 回退

```bash
cd /vllm-workspace/vllm-ascend
git checkout vllm_ascend/ops/fused_moe/token_dispatcher.py
git checkout vllm_ascend/ops/fused_moe/prepare_finalize.py
pip install -e .
rm -f /home/xuchi/A00282_tensors/*.pt
```

---

## 环境准备

### 通用环境（两节点容器内都执行）

```bash
# CANN / ATB 环境
source /usr/local/Ascend/ascend-toolkit/set_env.sh
source /usr/local/Ascend/cann-9.0.0/share/info/ascendnpu-ir/bin/set_env.sh
source /usr/local/Ascend/nnal/atb/set_env.sh

# LD_LIBRARY_PATH
export LD_LIBRARY_PATH=/usr/local/Ascend/ascend-toolkit/latest/python/site-packages:$LD_LIBRARY_PATH
export LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH
```

确认环境：

```bash
npu-smi info
cat "/usr/local/Ascend/ascend-toolkit/latest/$(uname -i)-linux/ascend_toolkit_install.info"
pip list | grep vllm
```

### Node 0（Leader）— 80.48.37.120

使用 2 节点 × 8 张 NPU，TP=8, DP=2。

```bash
# === 环境变量 ===
export VLLM_USE_MODELSCOPE=false
export OMP_PROC_BIND=false
export OMP_NUM_THREADS=1
export HCCL_BUFFSIZE=1024
export SERVER_PORT=8080
export NUMEXPR_MAX_THREADS=128
export TASK_QUEUE_ENABLE=1
export PYTORCH_NPU_ALLOC_CONF=expandable_segments:True
export HF_HUB_OFFLINE=1
export VLLM_ENGINE_READY_TIMEOUT_S=1800
export VLLM_LOGGING_LEVEL=INFO
export GLOG_minloglevel=1
export VLLM_WORKER_MULTIPROC_METHOD=spawn

# === 网络相关 ===
export MASTER_IP=80.48.37.120
export LOCAL_IP=80.48.37.120
export HCCL_IF_IP=80.48.37.120
export NIC_NAME=enp189s0f0
export HCCL_SOCKET_IFNAME=enp189s0f0
export GLOO_SOCKET_IFNAME=enp189s0f0
export TP_SOCKET_IFNAME=enp189s0f0

# === 启动 ===
vllm serve "/home/data/Qwen3-235B-A22B" \
    --host 0.0.0.0 \
    --port $SERVER_PORT \
    --served-model-name qwen \
    --data-parallel-size 2 \
    --data-parallel-size-local 1 \
    --data-parallel-address $LOCAL_IP \
    --data-parallel-rpc-port 13389 \
    --tensor-parallel-size 8 \
    --seed 1024 \
    --enable-expert-parallel \
    --max-num-seqs 128 \
    --max-model-len 40960 \
    --max-num-batched-tokens 2048 \
    --trust-remote-code \
    --gpu-memory-utilization 0.9 \
    --async-scheduling \
    --safetensors-load-strategy prefetch
```

### Node 1（Worker/headless）— 80.48.37.121

```bash
# === 环境变量 ===
export VLLM_USE_MODELSCOPE=false
export OMP_PROC_BIND=false
export OMP_NUM_THREADS=1
export HCCL_BUFFSIZE=1024
export SERVER_PORT=8080
export NUMEXPR_MAX_THREADS=128
export TASK_QUEUE_ENABLE=1
export PYTORCH_NPU_ALLOC_CONF=expandable_segments:True
export HF_HUB_OFFLINE=1
export VLLM_ENGINE_READY_TIMEOUT_S=1800
export VLLM_LOGGING_LEVEL=INFO
export GLOG_minloglevel=1
export VLLM_WORKER_MULTIPROC_METHOD=spawn

# === 网络相关 ===
export MASTER_IP=80.48.37.120     # Leader IP
export LOCAL_IP=80.48.37.121      # 本机 IP
export HCCL_IF_IP=80.48.37.121
export NIC_NAME=enp189s0f0
export HCCL_SOCKET_IFNAME=enp189s0f0
export GLOO_SOCKET_IFNAME=enp189s0f0
export TP_SOCKET_IFNAME=enp189s0f0

# === 启动 ===
vllm serve "/mnt/weight/Qwen3-235B-A22B" \
    --headless \
    --served-model-name qwen \
    --data-parallel-size 2 \
    --data-parallel-size-local 1 \
    --data-parallel-start-rank 1 \
    --data-parallel-address $MASTER_IP \
    --data-parallel-rpc-port 13389 \
    --tensor-parallel-size 8 \
    --seed 1024 \
    --enable-expert-parallel \
    --max-num-seqs 128 \
    --max-model-len 40960 \
    --max-num-batched-tokens 2048 \
    --trust-remote-code \
    --gpu-memory-utilization 0.9 \
    --async-scheduling \
    --safetensors-load-strategy prefetch
```

### 启动顺序

1. **先起 Node 1（Worker）** — headless 节点先启动，等待 Leader
2. **再起 Node 0（Leader）** — Leader 启动后自动与 Worker 建立 DP 连接

---

## 验证

### 单请求（预期正常，问题只在多并发出现）

```bash
curl http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "qwen",
    "messages": [
      {"role": "user", "content": "Janet has 3 apples. She buys 5 more apples at the store. Then she gives 2 apples to her friend. How many apples does Janet have now?"}
    ],
    "max_tokens": 500
  }'
```

### 32 并发验证

```bash
rm -rf /tmp/curl_*.json
for i in $(seq 1 32); do
  curl -s http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "qwen",
    "messages": [{"role": "user", "content": "Janet has 3 apples. She buys 5 more apples at the store. Then she gives 2 apples to her friend. How many apples does Janet have now?"}],
    "max_tokens": 500
  }' > /tmp/curl_$i.json &
done
wait

for i in $(seq 1 32); do
  echo "==================== Request $i ===================="
  cat /tmp/curl_$i.json
  echo ""
done
```

正常应输出带 CoT 推理过程的回答，最终答案为 `6`。

### Accuracy Benchmark (ais_bench)

前置条件：在 **Node 0（Leader）** 上执行（只需装一次）。

```bash
# 安装 vllm-ascend + ais_bench
cd /vllm-workspace/vllm-ascend
pip install -r requirements-dev.txt
pip install -e .
cd benchmark
pip install -e . -r requirements/api.txt -r requirements/extra.txt
```

创建配置并跑 accuracy：

```bash
export BENCHMARK_HOME="/tmp/aisbench_work"
mkdir -p $BENCHMARK_HOME/ais_bench/benchmark/configs/{models/vllm_api,datasets/gsm8k,summarizers}
mkdir -p $BENCHMARK_HOME/ais_bench/datasets

# 请求配置
cat > $BENCHMARK_HOME/ais_bench/benchmark/configs/models/vllm_api/vllm_api_general_chat_custom.py << 'EOF'
from ais_bench.benchmark.models import VLLMCustomAPIChat
from ais_bench.benchmark.utils.postprocess.model_postprocessors import extract_non_reasoning_content

models = [
    dict(
        attr="service",
        type=VLLMCustomAPIChat,
        abbr="vllm-api-general-chat",
        path="/home/data/Qwen3-235B-A22B",
        model="qwen",
        stream=False,
        request_rate=0,
        use_timestamp=False,
        retry=2,
        api_key="",
        host_ip="80.48.37.120",
        host_port=8080,
        url="",
        max_out_len=7680,
        batch_size=256,
        trust_remote_code=True,
        generation_kwargs=dict(temperature=0.6, ignore_eos=False),
        pred_postprocessor=dict(type=extract_non_reasoning_content),
    )
]
EOF

# 拷贝数据集 + summarizer 配置
PYTHON_LIB=$(python -c "import ais_bench; import os; print(os.path.dirname(ais_bench.__file__))")
cp $PYTHON_LIB/../ais_bench/benchmark/configs/datasets/gsm8k/gsm8k_gen_0_shot_cot_chat_prompt.py \
   $BENCHMARK_HOME/ais_bench/benchmark/configs/datasets/gsm8k/
cp $PYTHON_LIB/../ais_bench/benchmark/configs/summarizers/example.py \
   $BENCHMARK_HOME/ais_bench/benchmark/configs/summarizers/

# 跑 accuracy
BENCHMARK_HOME="/tmp/aisbench_work" \
ais_bench --models vllm_api_general_chat_custom \
          --datasets gsm8k_gen_0_shot_cot_chat_prompt \
          --debug 2>&1 | tee /tmp/benchmark_acc.log
```

**预期**：

- **正常**：~96.875%（2026-05-09 成功值）
- **异常**：0.0% / 3.125%（当前失败值）
- **threshold**：acc ≥ 91% 算通过

---

## 根因分析

### 已排除的方向

| 方向 | 排除依据 |
|------|---------|
| mc2_mask stale | 普通 print 证明 mask 设置在 `__enter__`（capture scope 外），每次正确更新 |
| prefix-caching | 默认 + 关 prefix-caching 不恢复 |
| 量化（BF16 vs W8A8） | w8a8 一样挂 |
| decode cudagraph | FULL_DECODE_ONLY 不恢复 |
| MC2 dispatch 参数差异 | 所有 A2 模型走相同 uniform 模式（global_bs=0），参数一致 |
| moe_forward vs moe_forward_shared | 两者共享 layer.forward_impl 的 MC2 核心逻辑 |

### 当前可疑点：self.num_tokens 实例属性

`PrepareAndFinalizeWithMC2.prepare`（`prepare_finalize.py`）设置：

```python
self.num_tokens, _ = hidden_states.shape   # Python int
```

`PrepareAndFinalizeWithAll2All.finalize` 使用：

```python
if self.num_tokens < hidden_states.shape[0]:
    hidden_states = hidden_states[: self.num_tokens]   # 用实例属性做 slice
```

如果 prepare 和 finalize 都在 cudagraph capture scope 内：

- capture 时：prepare 执行 → `self.num_tokens` 设为 capture_size
- replay 时：prepare 不执行 → `self.num_tokens` 保持上一次的值
- finalize 用陈旧的 `self.num_tokens` 做 slice → 输出错乱

**能解释的观察**：

- 单并发对（始终同一个 capture_size，num_tokens 一致）
- 多并发错（不同 size 交替，num_tokens 可能陈旧）
- 只有全 NONE 恢复（prepare 每次 Python 执行，num_tokens 正确）

**待 E7 确认**：用 `save_npugraph_tensor` 对比 prepare 和 finalize 的 num_tokens 值。

### nightly A2 配置对比

| 配置项 | Qwen3-235B-A22B | DeepSeek-R1 | GLM-5.1 | Kimi-K2.5 |
|--------|:-:|:-:|:-:|:-:|
| cudagraph_mode | 默认（PIECEWISE/FULL_AND_PIECEWISE） | FULL_DECODE_ONLY | FULL_DECODE_ONLY | FULL_DECODE_ONLY |
| prefix-caching | 开 | 关 | 关 | 关 |
| **speculative** | **无** | **mtp** | **mtp** | **eagle3** |
| quantization | BF16/W8A8 | W8A8 | W8A8 | W4A8 |

**关键差异**：Qwen3 是唯一无 speculative decode 的模型。speculative 可能改变了 decode 的 batch 结构，间接影响 cudagraph 的 capture/replay 行为。

### 间接证据（E1/E3）

| 实验 | 改动 | 结果 | 说明 |
|------|------|------|------|
| E1 | 绕开 MC2 走 ALLGATHER | ✅ 恢复 | MC2 路径有问题 |
| E3 | 关 cudagraph | ✅ 恢复 | cudagraph 是必要条件 |

### 为什么 GLM-5.1 / Kimi-K2.5 不显性失败

待确认。可能原因：

1. speculative decode 改变了 batch 结构，避免了不同 capture_size 的交替
2. 有 shared experts 补偿了 routed_out 的错误
3. 走了不同的代码路径（待排查）

### 依赖环境排除

5/9 成功版与 5/10 失败版的 CANN（8.5.1）、triton-ascend（3.2.0）、npu-smi（25.5.2）完全一致，非环境因素。

---

## 注意事项

- 两个节点之间需互通 **8080**（API）和 **13389**（DP RPC）端口
- 权重路径：Node 0 = `/home/data/Qwen3-235B-A22B`，Node 1 = `/mnt/weight/Qwen3-235B-A22B`
- `--async-scheduling` 对齐 5/9 成功日志（A2 nightly 一直使用）
- `--safetensors-load-strategy prefetch` 非 5/9 原始参数，仅加速权重加载；老版本若报 `unrecognized arguments` 可去掉
- **CWD 陷阱**：起 serve 前不要 `cd /vllm-workspace`，否则 `vllm` 被 Python 当相对目录解析 → `ImportError: cannot import name 'SamplingParams'`。改用 `cd /root`
- **NPU 清理**：`npu-smi info` 看卡上 Process id → `kill -9 <pid>`。docker stop 容器后 HBM 自动释放
- **绿区用 6/10 之后版本**：默认 `cudagraph_mode=FULL_AND_PIECEWISE`（O2），capture_sizes 有 19 个（`[1,2,4,...,128]`），拉起 capture 阶段比 5/9 版本（3 个 size）久一些，不影响正确性
- **改代码后确认生效**：绿区直接改 site-packages 里的文件即可（不用 `pip install -e .`），但改完要确认实际加载路径：`python3 -c "import vllm_ascend.ascend_forward_context as m; print(m.__file__)"` 应指向你改的文件
