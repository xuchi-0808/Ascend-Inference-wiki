# A00282 — 绿区操作手册

> 纯操作手册：serve 命令、验证脚本、切版本/重装命令。
> 当前任务/进度见 `task_plan.md`，根因分析见 `findings.md`，实验日志见 `progress.md`。

## 环境

- 镜像：4/28 镜像（CANN 8.5.1 + 驱动 25.5.0 C23）
- Node 0 = `80.48.37.227`（Leader）/ Node 1 = `80.48.37.228`（Worker）
- 权重：`/mnt/share/weights/Qwen3-235B-A22B-W8A8`
- 网卡：`enp189s0f0`（RoCE）

---

## 环境准备

### 通用环境（两节点容器内都执行）

```bash
source /usr/local/Ascend/ascend-toolkit/set_env.sh
source /usr/local/Ascend/nnal/atb/set_env.sh
export LD_LIBRARY_PATH=/usr/local/Ascend/ascend-toolkit/latest/python/site-packages:$LD_LIBRARY_PATH
export LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH
```

### 清设备（每次拉起前都做）

```bash
npu-smi info
for pid in $(npu-smi info | grep -oP 'PID:\s+\K\d+'); do kill -9 $pid 2>/dev/null; done
sleep 10
npu-smi info
```

> 507033（设备初始化失败）的根因 = 残留进程占用。每次拉起前必须清。

---

## serve 命令

### Node 0（Leader）— 80.48.37.227

```bash
# === 环境变量 ===
export VLLM_USE_MODELSCOPE=false
export OMP_PROC_BIND=false
export OMP_NUM_THREADS=1
export HCCL_BUFFSIZE=1024
export SERVER_PORT=8080
export NUMEXPR_MAX_THREADS=128
export TASK_QUEUE_ENABLE=1
export PYTORCH_NPU_ALLOC_CONF=expandable_segments:True
export HF_HUB_OFFLINE=1
export VLLM_ENGINE_READY_TIMEOUT_S=1800
export VLLM_LOGGING_LEVEL=INFO
export GLOG_minloglevel=1
export VLLM_WORKER_MULTIPROC_METHOD=spawn

# === 网络相关 ===
export MASTER_IP=80.48.37.227
export LOCAL_IP=80.48.37.227
export HCCL_IF_IP=80.48.37.227
export NIC_NAME=enp189s0f0
export HCCL_SOCKET_IFNAME=enp189s0f0
export GLOO_SOCKET_IFNAME=enp189s0f0
export TP_SOCKET_IFNAME=enp189s0f0

# === 启动 ===
vllm serve "/mnt/share/weights/Qwen3-235B-A22B-W8A8" \
    --host 0.0.0.0 \
    --port $SERVER_PORT \
    --served-model-name qwen \
    --data-parallel-size 2 \
    --data-parallel-size-local 1 \
    --data-parallel-address $LOCAL_IP \
    --data-parallel-rpc-port 13389 \
    --tensor-parallel-size 8 \
    --seed 1024 \
    --enable-expert-parallel \
    --max-num-seqs 128 \
    --max-model-len 40960 \
    --max-num-batched-tokens 2048 \
    --trust-remote-code \
    --gpu-memory-utilization 0.9 \
    --async-scheduling \
    --safetensors-load-strategy prefetch
```

### Node 1（Worker/headless）— 80.48.37.228

```bash
# === 环境变量 ===
export VLLM_USE_MODELSCOPE=false
export OMP_PROC_BIND=false
export OMP_NUM_THREADS=1
export HCCL_BUFFSIZE=1024
export SERVER_PORT=8080
export NUMEXPR_MAX_THREADS=128
export TASK_QUEUE_ENABLE=1
export PYTORCH_NPU_ALLOC_CONF=expandable_segments:True
export HF_HUB_OFFLINE=1
export VLLM_ENGINE_READY_TIMEOUT_S=1800
export VLLM_LOGGING_LEVEL=INFO
export GLOG_minloglevel=1
export VLLM_WORKER_MULTIPROC_METHOD=spawn

# === 网络相关 ===
export MASTER_IP=80.48.37.227
export LOCAL_IP=80.48.37.228
export HCCL_IF_IP=80.48.37.228
export NIC_NAME=enp189s0f0
export HCCL_SOCKET_IFNAME=enp189s0f0
export GLOO_SOCKET_IFNAME=enp189s0f0
export TP_SOCKET_IFNAME=enp189s0f0

# === 启动 ===
vllm serve "/mnt/share/weights/Qwen3-235B-A22B-W8A8" \
    --headless \
    --served-model-name qwen \
    --data-parallel-size 2 \
    --data-parallel-size-local 1 \
    --data-parallel-start-rank 1 \
    --data-parallel-address $MASTER_IP \
    --data-parallel-rpc-port 13389 \
    --tensor-parallel-size 8 \
    --seed 1024 \
    --enable-expert-parallel \
    --max-num-seqs 128 \
    --max-model-len 40960 \
    --max-num-batched-tokens 2048 \
    --trust-remote-code \
    --gpu-memory-utilization 0.9 \
    --async-scheduling \
    --safetensors-load-strategy prefetch
```

### 启动顺序

1. **先起 Node 1（Worker）** — headless 节点先启动，等待 Leader
2. **再起 Node 0（Leader）** — Leader 启动后自动与 Worker 建立 DP 连接

---

## 32 并发验证

等 Node 0 日志出现 `Starting vLLM server on http://0.0.0.0:8080`：

```bash
rm -rf /tmp/curl_*.json
for i in $(seq 1 32); do
  curl -s http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "qwen",
    "messages": [{"role": "user", "content": "Janet has 3 apples. She buys 5 more apples at the store. Then she gives 2 apples to her friend. How many apples does Janet have now?"}],
    "max_tokens": 500
  }' > /tmp/curl_$i.json &
done
wait

for i in $(seq 1 32); do
  echo "==================== Request $i ===================="
  cat /tmp/curl_$i.json
  echo ""
done
```

**判据**（curl 只能看乱码，不直接给 acc）：

| 结果 | 含义 |
|------|------|
| 32 个回答都带 CoT 推理，最终答案 `6`（无乱码） | ✅ 该版本精度正常 |
| 输出乱码 / 答案错乱 | ❌ 该版本有问题 |

---

## bisect 实验操作

### 兼容性地图

vllm-ascend 版本和 vllm 版本强耦合（MoERunner 结构依赖），不是任意组合都能跑：

| vllm-ascend | 兼容的 vllm 范围 | 原因 |
|---|---|---|
| 68ff5263（5/9） | `d886c26d4..726efe177` | 需要 DefaultMoERunner + SharedFusedMoE |
| 7fd2cede / ca4065f2（5/10） | 只有 `4d51588e2` | 需要 MoERunner 完整 + input_ids 参数 |

> `5e584ce9e`（#35782 移除 SharedFusedMoE）在两个兼容范围之间，无法单独测试。

### 实验组合

| 实验 | vllm-ascend | vllm | 目标 | 状态 |
|---|---|---|---|---|
| ✅ 基线 | 68ff5263 | d886c26d4 | 5/9 成功 | ✅ 已确认无乱码 |
| ❌ 对照 | ca4065f2 | 4d51588e2 | 5/10 失败 | ✅ 已确认复现乱码 |
| **实验 1** | **68ff5263** | **726efe177** | 测 #35949（reduce 移入 MoERunnerBase） | ⏳ 进行中 |

### 实验 1：vllm-ascend 68ff5263 + vllm 726efe177

#### 切版本（两节点都做）

```bash
cd /home/xuchi/vllm && git checkout 726efe177
cd /home/xuchi/vllm-ascend && git checkout 68ff5263
```

#### 初始化顺序修复（必须做！）

vllm 726efe177 的 `qwen3_moe.py` 改用 `SharedFusedMoE`，触发 vllm-ascend 68ff5263 的初始化顺序 bug：

```text
AttributeError: 'AscendSharedFusedMoE' object has no attribute '_routed_input_transform'
```

**原因**：`AscendSharedFusedMoE.__init__` 先调 `AscendFusedMoE.__init__`（内部访问 `self._routed_input_transform`），后设这个属性。

**修复**：把 `self._routed_input_transform = routed_input_transform` 移到 `AscendFusedMoE.__init__` 之前。

文件：`vllm_ascend/ops/fused_moe/fused_moe.py`，`AscendSharedFusedMoE.__init__` 方法里（约 line 573 附近）。

修改前：

```python
        AscendFusedMoE.__init__(self, **kwargs)
        if ascend_config.mix_placement:
            rocm_aiter_ops.is_fusion_moe_shared_experts_enabled = mock_true
            rocm_aiter_ops.is_fused_moe_enabled = mock_true

        self._routed_input_transform = routed_input_transform
```

修改后：

```python
        self._routed_input_transform = routed_input_transform
        AscendFusedMoE.__init__(self, **kwargs)
        if ascend_config.mix_placement:
            rocm_aiter_ops.is_fusion_moe_shared_experts_enabled = mock_true
            rocm_aiter_ops.is_fused_moe_enabled = mock_true
```

> 这个修复只调整初始化顺序，不改 MC2/cudagraph 逻辑，不影响实验判据。

#### 重装（两节点都做）

```bash
cd /home/xuchi/vllm && pip install -e . --no-build-isolation
cd /home/xuchi/vllm-ascend && pip install -e . --no-build-isolation
```

#### triton 兼容 workaround（如遇报错）

```bash
cd /home/xuchi/vllm-ascend
sed -i 's/if HAS_TRITON:/if True:/' vllm_ascend/ops/__init__.py
pip install -e . --no-build-isolation
```

#### 拉起 + 验证

1. 清设备：`npu-smi info` + kill 残留进程
2. Node 1 先起，Node 0 后起
3. 等 Node 0 日志出现 `Starting vLLM server on http://0.0.0.0:8080`
4. 跑 32 并发 curl（见「32 并发验证」章节）

#### 判读

| 结果 | 含义 |
|---|---|
| ✅ 无乱码 | #35949 不是问题，进入实验 2（测 #40560） |
| ❌ 乱码 | #35949 引入了 bug |

### 后续实验（如实验 1 正常）

实验 2 需要改 vllm-ascend 代码（去掉 `input_ids` 参数的硬依赖），解锁中间 vllm 版本。详见 `task_plan.md` 的「bisect 实验组合」。

---

## 已排除方向（代码级 + 实验级确认）

以下方向已确认排除，**在内网不要再试**：

| 方向 | 排除依据 |
|------|---------|
| **版本 bisect** | vllm-ascend 和 vllm 强耦合，3 个关键 PR（#35782/#35949/#40560）全在兼容空隙里。实验 1（68ff5263+726efe177）报 `AttributeError: reduce_results`（#35949 删了该属性），不只是初始化顺序问题 |
| **HCCL_BUFFSIZE** | 代码级确认：MC2 dispatch 算子校验通过后 BUFFSIZE 不影响正确性（只影响通信窗口大小）。实测 BUFFSIZE=200 精度仍不对。详见 ops-transformer 仓库 `mc2/` 的 tiling 代码 |
| **Python 代码层面差异** | 逐行对比 5/9 和 5/10 的 forward() tensor op 序列，对 Qwen3+MC2 完全等效。MC2 核心（dispatch/combine）、reduce、trunc 全一致。唯一差异是 custom op schema（4参数→7参数），但这在 Python 层面不产生 tensor op 差异 |
| **prefix-caching** | 关了不恢复（已实测） |
| **量化（BF16 vs W8A8）** | 都挂（已实测） |
| **decode cudagraph** | FULL_DECODE_ONLY 不恢复（已实测） |

## 当前结论

**精度差异的根因在 Python 代码层面以下**（aclgraph capture/replay 机制、custom op schema 间接影响、或 C++ kernel）。需要用 `save_npugraph_tensor` 或反向实验定位，不是代码审查能解决的。

## 下一步方向

详见 `task_plan.md` 的「下一步方向」章节。推荐方向：
1. **save_npugraph_tensor 诊断**：在 MC2 路径关键节点 save tensor，定位哪个环节输出异常
2. **反向实验 E8**：在 `MoERunner.forward()` 跳过中间操作，看精度是否恢复

---

## 注意事项

- 两个节点之间需互通 **8080**（API）和 **13389**（DP RPC）端口
- **CWD 陷阱**：起 serve 前不要 `cd /vllm-workspace`，否则 `vllm` 被 Python 当相对目录解析 → `ImportError`。改用 `cd /root`
- **NPU 清理**：每次拉起前必须清设备（避免 507033）
- **改代码后确认生效**：`python3 -c "import vllm_ascend.ascend_forward_context as m; print(m.__file__)"` 应指向你改的文件
- `--safetensors-load-strategy prefetch` 非 5/9 原始参数，仅加速权重加载；老版本若报 `unrecognized arguments` 可去掉
